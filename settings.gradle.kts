rootProject.name = "age-verification"

include(
        // data types
        "data",

        // api types
        "api",
        "testing-api",

        // api
        "infra-api",
        "common-api",
        "avs-api",
        "site-api",

        // service
        "infra-service",
        "common-service",
        "site-service",
        "common",
        "verification-poc",
        "test-service",

        // server
        "common-server",
        "test-server",
        "testing-server",

        // demo
        "demo",
)
